datasource db {
  provider = "postgresql"
}

generator client {
  provider = "prisma-client-js"
}

model ShortLink {
  id        String   @id @default(cuid())
  slug      String   @unique
  targetUrl String
  createdAt DateTime @default(now())
}
